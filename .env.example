# PostgreSQL (used by docker-compose and app when connecting to DB)
POSTGRES_USER=lightgcn
POSTGRES_PASSWORD=lightgcn
POSTGRES_DB=lightgcn
POSTGRES_PORT=5432

# Connection URL for the app (when you add DB support)
# DATABASE_URL=postgresql://lightgcn:lightgcn@localhost:5432/lightgcn
